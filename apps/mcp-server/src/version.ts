// Keep these in sync with package.json. The `leverie-mcp` CLI is small enough
// that a hardcoded constant is simpler than runtime package.json parsing and
// works identically in dev (tsx), under `pnpm test`, and from a published bin.
export const PACKAGE_NAME = 'leverie-mcp';
export const PACKAGE_VERSION = '0.1.0';
