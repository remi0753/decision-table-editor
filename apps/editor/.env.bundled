# Active when `vite build --mode bundled` runs. Produces the admin-only SPA
# bundled into @leverie/server (no marketing top page, admin-flavored
# index.html metadata, `/` redirects to /edit).
VITE_FLAVOR=bundled
