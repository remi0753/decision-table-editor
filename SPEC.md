# ディシジョンテーブル エディタ — 仕様書

> 対象ファイル: `decision-table-editor.html`  
> 実装形式: 依存ライブラリなし・単一HTMLファイル（HTML / CSS / JavaScript）

---

## 目次

1. [概要](#1-概要)
2. [データモデル](#2-データモデル)
3. [型システム](#3-型システム)
4. [演算子と値のバリデーション](#4-演算子と値のバリデーション)
5. [マッチングロジック](#5-マッチングロジック)
6. [画面レイアウト](#6-画面レイアウト)
7. [ヘッダー](#7-ヘッダー)
8. [ツールバー](#8-ツールバー)
9. [フィールド定義パネル](#9-フィールド定義パネル)
10. [ディシジョンテーブル](#10-ディシジョンテーブル)
11. [セルエディタポップアップ](#11-セルエディタポップアップ)
12. [値を評価するパネル](#12-値を評価するパネル)
13. [JSON エクスポート / インポートパネル](#13-json-エクスポート--インポートパネル)
14. [初期データ](#14-初期データ)
15. [キーボード操作](#15-キーボード操作)
16. [ID採番ルール](#16-id採番ルール)

---

## 1. 概要

ディシジョンテーブル（決定表）をブラウザ上でインタラクティブに編集・評価するためのツール。  
ルールは「条件列」と「結果」で構成される行の集合として表現される。  
行は上から順に評価され、**最初に全条件が一致した行の結果**が採用される（first-match セマンティクス）。

---

## 2. データモデル

### 2.1 グローバル状態オブジェクト `S`

```js
const S = {
    fields: [],  // フィールド定義リスト（後述）
    cols:   [],  // 条件列リスト（後述）
    rows:   [],  // ルール行リスト（後述）
    nField: 1,   // 次のフィールドID生成用カウンター
    nCol:   1,   // 次の列ID生成用カウンター
    nRow:   1,   // 次の行ID生成用カウンター
};
```

### 2.2 フィールド定義 `Field`

条件列に割り当てられる「名前と型」のマスターレコード。

```ts
{
  id:   string,  // "f" + 連番整数（例: "f1", "f2"）
  name: string,  // 表示名。S.fields 内でユニーク
  type: "number" | "string" | "bool" | "any"
}
```

- フィールド名はアプリ全体でユニーク（重複追加時はアラートを表示して拒否）。
- フィールドの編集（名前・型の変更）は現バージョンでは非対応。追加と削除のみ可能。

### 2.3 条件列 `Col`

テーブルの条件列を表す。フィールド定義を参照する。

```ts
{
  id:      string,       // "c" + 連番整数（例: "c1", "c2"）
  fieldId: string | null // 参照するフィールドの id。未選択時は null
}
```

- `fieldId` が `null` の列は「フィールド未選択」状態。セルの条件編集・評価パネルへの表示は行われない。

### 2.4 セル条件 `Cell`

各行と各列の交点に保持される条件。省略された交点はワイルドカードとして扱われる。

```ts
{
  op:  "=" | "<" | "<=" | ">" | ">=",
  val: string  // 保存値は常に文字列
}
```

- セル条件がない（`row.cells[colId]` が `undefined`）場合は、その列の条件を無視（ワイルドカード）。

### 2.5 ルール行 `Row`

```ts
{
  id:     string,                       // "r" + 連番整数（例: "r1", "r2"）
  cells:  { [colId: string]: Cell },    // 列IDをキーとするセル条件マップ
  result: string                        // 結果の文字列（空文字も許容）
}
```

---

## 3. 型システム

### 3.1 フィールド型の種類

| 型値 | 表示名 | バッジ色 |
|------|--------|---------|
| `"number"` | 数値 | 青（`#dbeafe` / `#1e40af`） |
| `"string"` | 文字列 | 緑（`#dcfce7` / `#15803d`） |
| `"bool"` | bool | 黄（`#fef3c7` / `#92400e`） |
| `"any"` | 任意 | グレー（`#f3f4f6` / `#6b7280`） |

### 3.2 フィールド型による入力UI切り替え

セルエディタと評価パネルの入力ウィジェットは、フィールドの型と選択中の演算子に応じて動的に切り替わる。

| フィールド型 | 演算子 | セルエディタの値入力 | 評価パネルの入力 |
|-------------|--------|---------------------|-----------------|
| `number` | 任意 | `<input type="number" step="any">` | `<input type="number" step="any">` |
| `string` | `=` のみ | `<input type="text">` (placeholder: "文字列を入力") | `<input type="text">` |
| `bool` | `=` のみ | `<select>` (true / false) | `<select>` (未指定 / true / false) |
| `any` | `=` のみ | `<input type="text">` (placeholder: "値を入力") | `<input type="text">` |
| 任意 | `<` `<=` `>` `>=` | `<input type="number" step="any">` に**強制上書き** | — |

**演算子による強制上書きルール**: 大小比較演算子（`<` `<=` `>` `>=`）が選択されると、フィールド型が何であれ値入力欄は数値入力に切り替わる（`bool` 型でも同様）。

### 3.3 値の型強制（coerce）

マッチング評価時に、文字列として保存された値を適切な型に変換する。

```
coerce(s):
  s == null           → ""（空文字列）
  s === "true"        → true（真偽値）
  s === "false"       → false（真偽値）
  Number(s) が有限値  → Number(s)（数値）
  それ以外            → s（文字列のまま）
```

---

## 4. 演算子と値のバリデーション

### 4.1 演算子一覧

| 内部値 | 表示記号 | 説明 |
|--------|---------|------|
| `""` | (なし) | 条件なし。セルはワイルドカードとして機能 |
| `"="` | = | 等しい（loose equality `==`） |
| `"<"` | < | より小さい（未満） |
| `"<="` | ≤ | 以下 |
| `">"` | > | より大きい（超過） |
| `">="` | ≥ | 以上 |

### 4.2 バリデーション

演算子が `<` `<=` `>` `>=` のいずれかで、かつ値フィールドが空でない場合：

- `Number(val)` が `NaN` であれば保存を拒否する。
- セルエディタ内にインラインエラーメッセージを表示する（赤文字、`#ef4444`）。
- メッセージ: `"大小比較（＜ ≤ ＞ ≥）には数値を入力してください"`
- 値フィールドにフォーカスを戻す。

演算子が `""` の場合は値の内容に関わらず、セル条件をそのまま削除（ワイルドカード化）する。

---

## 5. マッチングロジック

### 5.1 単一セルのマッチ判定

```
matches(cell, rawInput):
  cell が null または cell.op が空文字 → true（ワイルドカード）
  rawInput が null または "" → true（評価パネルで未入力の場合もワイルドカード扱い）
  a = coerce(rawInput)
  b = coerce(cell.val)
  cell.op に応じて:
    "="  → a == b  （型強制あり loose equality）
    "<"  → a < b
    "<=" → a <= b
    ">"  → a > b
    ">=" → a >= b
  いずれにも該当しない → false
```

### 5.2 行のマッチ判定

```
rowMatches(row):
  S.cols の全列 col に対して
    matches(row.cells[col.id], inputs[col.id]) が true
  であれば行全体が一致
```

### 5.3 評価実行（runEval）

1. 評価パネルの全入力値を `inputs` オブジェクトに収集（`colId → value`）。
2. `S.rows` を先頭から順に走査し、`rowMatches` が初めて `true` になった行を採用。
3. 該当行が見つかった場合：
   - テーブル上の該当行を黄色ハイライト（クラス `matched` を付与）。
   - 結果パネルを緑色（`.eval-result.ok`）で表示し、`row.result` を表示。
   - `row.result` が空の場合は `"(結果が未設定です)"` と表示。
4. 該当行が見つからない場合：
   - 結果パネルを赤色（`.eval-result.ng`）で表示し、`"どのルールにも一致しませんでした"` と表示。

---

## 6. 画面レイアウト

```
┌─────────────────────────────────────────────────┐
│ ヘッダー（グラデーション青）                        │
├─────────────────────────────────────────────────┤
│ メインエリア（padding: 24px、背景: #f0f2f5）      │
│ ┌───────────────────────────────────────────┐   │
│ │ ツールバー（flex、折り返しあり）              │   │
│ ├───────────────────────────────────────────┤   │
│ │ フィールド定義パネル（常時表示、カード形式）   │   │
│ ├───────────────────────────────────────────┤   │
│ │ ディシジョンテーブル（横スクロール対応）       │   │
│ ├───────────────────────────────────────────┤   │
│ │ ヘルプノート（グレーテキスト）                │   │
│ ├───────────────────────────────────────────┤   │
│ │ 値を評価するパネル（カード形式）              │   │
│ ├───────────────────────────────────────────┤   │
│ │ JSON エクスポート/インポートパネル（折りたたみ）│   │
│ └───────────────────────────────────────────┘   │
└─────────────────────────────────────────────────┘
         ↑ fixed overlay + セルエディタポップアップ
```

---

## 7. ヘッダー

- 背景: `linear-gradient(135deg, #1e40af, #3b82f6)`（青のグラデーション）
- タイトル: `"ディシジョンテーブル エディタ"`（22px bold）
- 説明文: 使い方の概要を2行で表示（13px、opacity 0.85）

---

## 8. ツールバー

| ボタン | スタイル | 動作 |
|--------|---------|------|
| `＋ 条件列を追加` | btn-blue | `addConditionCol()` → フィールド未選択の列を末尾に追加 |
| `＋ 行を追加` | btn-white | `addRowHandler()` → 空のルール行を末尾に追加 |

右端にヒントテキスト（`💡 条件セルをクリックして演算子と値を設定します`）を表示。

---

## 9. フィールド定義パネル

ツールバーとテーブルの間に**常時表示**されるカード形式パネル。  
条件列に割り当て可能なフィールドのマスターリストを管理する。

### 9.1 表示内容

- タイトル: `"フィールド定義"` (14px bold)
- サブタイトル: `"条件列に設定できる項目と型の一覧"` (12px グレー)
- フィールドチップ一覧（後述）
- フィールド追加フォーム（後述）

### 9.2 フィールドチップ

定義済みフィールドを1つのチップとして表示。

```
[ フィールド名  [型バッジ]  [×] ]
```

| 要素 | 詳細 |
|------|------|
| フィールド名 | 太字（600）、`#1f2937` |
| 型バッジ | 型に対応する背景色・文字色（§3.1参照）、10px 太字 |
| 削除ボタン `×` | 通常: グレー（`#d1d5db`）/ ホバー: 赤（`#ef4444`） |

**削除ボタンの無効化**: そのフィールドを `fieldId` として参照している列が1つでも存在する場合、削除ボタンは `disabled` 状態になる（クリック不可・薄表示）。無効状態でクリックしても何も起きない。

フィールドが1件もない場合は `"フィールドがまだ定義されていません"` というプレースホルダーテキストを表示。

### 9.3 フィールド追加フォーム

```
[ フィールド名テキスト入力 ]  [ 型セレクト ▼ ]  [ ＋ 追加 ]
```

| 要素 | 詳細 |
|------|------|
| フィールド名入力 | `type="text"`、placeholder: `"フィールド名"` |
| 型セレクト | 選択肢: 数値 / 文字列 / bool / 任意（デフォルト: 数値） |
| 追加ボタン | btn-blue、`addField()` を呼び出す |

**追加時のバリデーション:**
1. 名前が空文字（トリム後）の場合: フォーカスをテキスト入力に戻すのみ（エラー表示なし）。
2. 同名フィールドが既に存在する場合: `alert()` を表示して処理を中断。
3. 正常追加後: テキスト入力をクリア、フィールドパネルを再描画、テーブルヘッダーのドロップダウンを更新（`render()` を呼び出す）。

---

## 10. ディシジョンテーブル

### 10.1 テーブル全体の構造

白背景のカード（`border-radius: 10px`）内に `<table>` を配置。横スクロール対応。

```
┌──┬─────────────┬─────────────┬──────────────────┬──┐
│ # │ 条件列 A    │ 条件列 B    │ 結果（終端ノード）  │  │
├──┼─────────────┼─────────────┼──────────────────┼──┤
│ 1 │  [chip]    │  [chip]     │ [テキスト入力]    │✕│
│ 2 │  [chip]    │  [chip]     │ [テキスト入力]    │✕│
└──┴─────────────┴─────────────┴──────────────────┴──┘
```

列構成（左から順に）:
1. **番号列** (`.th-num` / `.td-num`): 44px幅、行番号表示（1始まり）
2. **条件列** (`.th-condition` / `.td-condition`): 1列以上、最小幅190px
3. **結果列** (`.th-result` / `.td-result`): 1列、最小幅190px
4. **削除列** (`.th-del` / `.td-del`): 40px幅、行削除ボタン

### 10.2 ヘッダー行（条件列）

```
┌──────────────────────────┐
│ [フィールド選択 ▼]    [×] │  ← .th-cond-inner
│ [型バッジ]                │  ← .th-cond-type
└──────────────────────────┘
```

| 要素 | 詳細 |
|------|------|
| フィールド選択ドロップダウン | フィールド定義パネルに登録されたフィールドの一覧を表示。先頭に `"(フィールドを選択)"` オプション。未選択時はテキストがライトブルー（`#93c5fd`）で細字表示 |
| 型バッジ | 選択中フィールドの型を色付きバッジで読み取り専用表示。未選択時は非表示 |
| 列削除ボタン `×` | 通常: ライトブルー（`#93c5fd`）/ ホバー: 赤背景 + 赤文字 |
| 左挿入ボタン `＋` | 列ホバー時に左辺に出現（`col-ins-left`）。クリックでこの列の左に新列を挿入 |
| 右挿入ボタン `＋` | 列ホバー時に右辺に出現（`col-ins-right`）。クリックでこの列の右に新列を挿入 |

**列削除時の制約**: 条件列が1列のみの場合は削除を拒否し、アラートを表示する。  
削除確認は `confirm()` ダイアログで行う。削除後、フィールドパネルの「使用中」状態を更新する。

**フィールド選択変更時**: `onColField(select)` → `col.fieldId` を更新 → フィールドパネルを再描画 → テーブル全体を再描画。

### 10.3 ヘッダー行（結果列）

- 背景: `#f0fdf4`（薄緑）
- テキスト: `"結果（終端ノード）"` (13px bold 緑 `#15803d`)

### 10.4 ボディ行

各行の番号セル（`.td-num`）には、行ホバー時に上下に行挿入ボタン `＋` が現れる。

| ボタン | 位置 | 動作 |
|--------|------|------|
| `.row-ins-before` | セル上辺 -9px | この行の**上**に新行を挿入 |
| `.row-ins-after` | セル下辺 -9px | この行の**下**に新行を挿入 |

### 10.5 条件セル（チップ）

各条件セルの状態と表示：

| 状態 | クラス | 見た目 | クリック |
|------|--------|--------|---------|
| フィールド未選択 | `.chip.no-field` | 薄グレー破線枠、"フィールド未選択"（斜体） | 不可（`cursor: default`） |
| 条件あり | `.chip.filled` | 青背景（`#dbeafe`）、演算子記号 + 値 | セルエディタを開く |
| 条件なし（ワイルドカード） | `.chip.empty` | 破線枠、"任意の値"（斜体グレー） | セルエディタを開く |

`.chip.filled` のホバー時: 背景が `#bfdbfe` に変化。  
`.chip.empty` のホバー時: 背景が `#f0f9ff`、枠・文字がライトブルーに変化。

### 10.6 結果セル

- `<input type="text">` が幅100%で配置。
- `oninput` / `onchange` で `row.result` をリアルタイム更新。
- フォーカス時に薄緑の枠と背景色（`#f0fdf4`）に変化。
- placeholder: `"結果の値を入力..."`

### 10.7 行削除ボタン

- 削除列（最右列）に `✕` ボタン。
- 確認ダイアログなしで即時削除。
- 通常: グレー（`#d1d5db`）/ ホバー: 赤（`#ef4444`）

### 10.8 マッチハイライト

評価実行後、一致した行のすべてのセルに `.matched` クラスが付与される。

- 条件セル・番号セル等: 背景 `#fef9c3`（薄黄）
- 結果セル: 背景 `#fde68a`（黄）
- 再評価またはクリア時にハイライトを解除。

---

## 11. セルエディタポップアップ

条件チップをクリックすると開く固定ポジションのポップアップ。

### 11.1 表示条件

- クリックした列に有効なフィールドが設定されていること。
- 未設定の場合は `alert("先に列ヘッダーでフィールドを選択してください")` を表示して中断。

### 11.2 構造

```
┌──────────────────────────────────┐
│ 「{フィールド名}」の条件を設定      │  ← ed-title
├──────────────────────────────────┤
│ 演算子                            │
│ [ セレクトボックス ▼ ]            │
├──────────────────────────────────┤
│ 値                                │
│ [ 入力フィールド（可変） ]         │
│ （エラーメッセージ）               │
├──────────────────────────────────┤
│ [ 適用 ] [ クリア ] [ キャンセル ] │
└──────────────────────────────────┘
```

### 11.3 位置計算

クリックされた `<td>` の `getBoundingClientRect()` を基準に配置。

- 基本: `td.bottom + 6px` を `top` とし、`td.left` を `left` とする。
- 画面下端に収まらない場合: `td.top - H - 6px` に切り替え（上方向に表示）。
- 画面右端に収まらない場合: `window.innerWidth - W - 12px` に制限。
- 上端・左端の最小値: 8px。
- ポップアップサイズ: W=288px、H=260px（計算上の想定値）。

### 11.4 オーバーレイ

ポップアップ背面にフルスクリーンの半透明オーバーレイ（`rgba(0,0,0,.15)`、z-index: 200）を表示。  
オーバーレイクリックでポップアップを閉じる（`closeEditor()`）。

### 11.5 演算子セレクト（`#ed-op`）

| 選択肢 | value |
|--------|-------|
| (条件なし — 任意の値に一致) | `""` |
| = 等しい | `"="` |
| < より小さい（未満） | `"<"` |
| ≤ 以下 | `"<="` |
| > より大きい（超過） | `">"` |
| ≥ 以上 | `">="` |

演算子変更時（`change` イベント）:
1. 現在の値入力フィールドの値を退避。
2. `updateEdValueField(field.type, newOp)` で入力フィールドの種類を切り替え。
3. 退避した値を新しいフィールドに復元（bool → bool なら一致する選択肢を選ぶ、それ以外は文字列を引き継ぐ）。

### 11.6 値入力フィールド（動的切り替え）

`updateEdValueField(colType, op)` が呼ばれるたびに、以下の3パターンのいずれかを表示し、他を非表示にする：

| 条件 | 表示する要素 |
|------|------------|
| `op` が比較演算子 OR `colType === "number"` | `#ed-val`（`type="number"`, `step="any"`, placeholder: "数値を入力"） |
| `colType === "bool"` | `#ed-val-bool`（`<select>`: true / false） |
| それ以外 | `#ed-val`（`type="text"`, placeholder: "文字列を入力" or "値を入力"） |

### 11.7 ボタン動作

| ボタン | 動作 |
|--------|------|
| 適用 | `applyEditor()`: バリデーション通過後、`row.cells[colId]` を更新（演算子空なら削除）。ポップアップを閉じ、テーブルを再描画。 |
| クリア | `clearCellEditor()`: `row.cells[colId]` を削除（ワイルドカード化）。ポップアップを閉じ、再描画。 |
| キャンセル | `closeEditor()`: 変更なしでポップアップを閉じる。 |

### 11.8 初期フォーカス

ポップアップを開いた後 40ms のタイマーで、テキスト/数値入力欄（`#ed-val`）が表示されている場合はフォーカスを移動する。bool セレクトが表示されている場合はフォーカスを移動しない。

---

## 12. 値を評価するパネル

### 12.1 概要

テーブル下部のカード形式パネル。各条件列に対応する入力欄に値を入力し、どのルールが適用されるか確認できる。

### 12.2 入力フィールドの動的生成（`renderEvalFields`）

`S.cols` を走査し、`fieldId` が設定されている列に対してのみ入力欄を生成（`fieldId` が null の列はスキップ）。

フィールド型に応じた入力ウィジェット:

| フィールド型 | 生成される入力要素 |
|-------------|------------------|
| `number` | `<input type="number" step="any">` |
| `bool` | `<select>`: 選択肢は「（未指定）/ true / false」 |
| `string` / `any` | `<input type="text">` |

再描画時に既存の入力値を保持する（`data-evalcol` 属性でキー管理）。

### 12.3 ボタン

| ボタン | スタイル | 動作 |
|--------|---------|------|
| ▶ 評価する | btn-green | `runEval()` |
| クリア | btn-white | `clearEval()` |

`clearEval()`: 全入力欄の値を空文字にリセット、マッチハイライトを解除、結果表示を非表示化。

### 12.4 結果表示

| 状態 | クラス | 背景色 | 表示内容 |
|------|--------|--------|---------|
| 非表示（初期） | `.eval-result` | — | — |
| 一致 | `.eval-result.show.ok` | 緑（`#dcfce7`） | ラベル: "一致 → 結果"、値: `row.result` |
| 不一致 | `.eval-result.show.ng` | 赤（`#fee2e2`） | ラベル: "不一致"、値: "どのルールにも一致しませんでした" |

---

## 13. JSON エクスポート / インポートパネル

### 13.1 概要

折りたたみ式パネル（初期状態: 閉）。`"▾"` アイコンの回転でトグル表示。

### 13.2 エクスポート

`exportJson()` が呼ばれると、以下の構造を `JSON.stringify(obj, null, 2)` でフォーマットしてテキストエリアに書き出す。

```json
{
  "fields": [
    { "id": "f1", "name": "購入金額", "type": "number" },
    { "id": "f2", "name": "会員種別", "type": "string" }
  ],
  "cols": [
    { "id": "c1", "fieldId": "f1" },
    { "id": "c2", "fieldId": "f2" }
  ],
  "rows": [
    {
      "id": "r1",
      "cells": {
        "c1": { "op": ">=", "val": "10000" },
        "c2": { "op": "=",  "val": "premium" }
      },
      "result": "20%割引"
    }
  ]
}
```

### 13.3 インポート

`importJson()` が呼ばれると、テキストエリアの内容を `JSON.parse()` し、以下を検証・処理する。

**必須チェック**: `cols` と `rows` が配列であること。違反時は `alert()` でエラーを表示。

**フォーマット判定と処理:**

| 条件 | フォーマット | 処理 |
|------|------------|------|
| `fields` 配列が存在する | 現行フォーマット | `S.fields = d.fields; S.cols = d.cols;` そのまま適用 |
| `fields` がない | レガシーフォーマット | 下記の自動マイグレーション |

**レガシー自動マイグレーション**: `cols[i].name` と `cols[i].type` からフィールド定義を再構築する。同名・同型のフィールドは1件にまとめ、重複しない場合のみ新規追加する。`cols[i]` は `{ id, fieldId }` 形式に変換される。

**IDカウンターの再計算**: インポート後、`S.nField`, `S.nCol`, `S.nRow` をインポートデータの最大ID番号+1 に更新し、ID重複を防ぐ。

**完了通知**: `alert("インポートが完了しました")` を表示。

---

## 14. 初期データ

ページロード時に以下のデータを自動生成する。

**フィールド定義:**

| id | name | type |
|----|------|------|
| f1 | 購入金額 | number |
| f2 | 会員種別 | string |

**条件列:**

| id | fieldId |
|----|---------|
| c1 | f1 |
| c2 | f2 |

**ルール行（顧客割引テーブル）:**

| # | 購入金額 (c1) | 会員種別 (c2) | 結果 |
|---|-------------|-------------|------|
| 1 | ≥ 10000 | = premium | 20%割引 |
| 2 | ≥ 10000 | (ワイルドカード) | 15%割引 |
| 3 | ≥ 5000 | (ワイルドカード) | 10%割引 |
| 4 | ≥ 1000 | (ワイルドカード) | 5%割引 |
| 5 | (ワイルドカード) | (ワイルドカード) | 割引なし |

---

## 15. キーボード操作

| キー | 対象 | 動作 |
|------|------|------|
| `Enter` | フィールド追加フォームの名前入力（`#field-add-name`） | `addField()` を呼び出す |
| `Enter` | セルエディタの値入力（`#ed-val`） | `applyEditor()` を呼び出す |
| `Escape` | セルエディタの値入力（`#ed-val`） | `closeEditor()` を呼び出す |

---

## 16. ID採番ルール

| 種別 | フォーマット | カウンター |
|------|------------|-----------|
| フィールド | `"f"` + `S.nField++` | `S.nField`（初期値: 1） |
| 条件列 | `"c"` + `S.nCol++` | `S.nCol`（初期値: 1） |
| ルール行 | `"r"` + `S.nRow++` | `S.nRow`（初期値: 1） |

インポート時にはカウンターをインポートデータ内の最大連番+1 に設定し直す。これにより、インポート後に追加するエンティティのIDが既存のIDと衝突しないことを保証する。
